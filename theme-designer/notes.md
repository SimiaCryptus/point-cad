update [analyze.op.md](../demo/analyze.op.md) and [theme.html](../demo/theme.html): instead of a markdown output we have to cut and paste, we want to transition to an html format that auto-encapsulates all the needed fields as an enhanceent to the existing [theme.html](../demo/theme.html) demo harness. it should be sure to include fields that describe the migration guide, and provide it via the ui so the user can play with the colors, produce optimized themes, save the css, and also save an md file which has instructions for incorporation for use by either a human or an agent


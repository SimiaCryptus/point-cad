- ~~auto-normalize the optimization so that free/unanchored global location and orientation are not considered unconstrained~~ — done: `solver … frame auto` (default) re-centres the solved cloud and aligns it to its principal axes when ≤ 1 point is fixed (a single anchor is the pivot); rigid-body DOF are reported as gauge-fixed, not as slack. `center` command / "Center" button applies the same to seeds. `frame none` restores the old behaviour.
- ~~Variables / Points / Constraints / Script / Solve should each be free floating tool windows that can appear or disappear by pressing/unpressing a toolbar button~~ — done: draggable, resizable windows over the viewport; `open="…"` attribute picks the initial set, `showWindow/hideWindow/toggleWindow` API.
- ~~improve orbit/view controls~~ — done: turntable orbit with elevation clamp, zoom-to-cursor wheel, ctrl-drag dolly, pinch/pan on touch, keyboard (arrows, ±, F, R, 0/1/3/7/5), view presets, clickable axis gizmo, double-click to focus/fit, debounced camera commits.
- ~~theme designer (t1)~~ — done as `extensions/theme/` + `demo/theme.html`. Core grew the generic pieces from `theme-designer/README.md` §6: inequality targets (`>=`, `<=`, hinge residuals), vector-valued measures + variadic/parameterised kinds (`lock L a b c`), scenarios (`scenario`/`theme` blocks, `var x shared`, `solve scenarios`, stacked problem, `result.perScenario`, `el.activeScenario`), space capabilities (`requires`), registered statements / point roles / implicit constraints, colour literals via `space.parseLiteral`. Not yet: linked measures (`@ID`, §6.1), `repeat`, tokens import, Display-P3, CVD.
- ideas: remember window layout per host (localStorage), inertia on orbit, "closest to seeds" frame mode as a middle ground between `auto` and `none`
